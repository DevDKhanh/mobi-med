import 'package:flutter/material.dart';

class AppConst {
  static String version = 'Version: 1.0.22';

  static fullWidth(context) => MediaQuery.of(context).size.width;
  static fullHeight(context) => MediaQuery.of(context).size.height;
}
